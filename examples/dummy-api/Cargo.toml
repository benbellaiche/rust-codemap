[package]
name = "dummy-api"
version = "0.1.0"
edition = "2021"

[dependencies]
dummy-core = { path = "../dummy-core" }
tracing = "0.1"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros", "time", "sync"] }
